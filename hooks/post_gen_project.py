"""
Does the following:

1. Nothing so far... but will set up gh-page branch if user wants it

"""

import os
from cookiecutter.config import DEFAULT_CONFIG

# Get the root project directory
PROJECT_DIRECTORY = os.path.realpath(os.path.curdir)
